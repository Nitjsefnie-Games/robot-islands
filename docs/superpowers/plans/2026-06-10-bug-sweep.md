# Bug Sweep 2026-06-10 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Fix every confirmed defect found by the 2026-06-10 eight-agent audit sweep (~60 fixes across economy, data tables, persistence, logistics, skill tree, world/merge, orbital/weather, and UI), keeping the suite green and master linear.

**Architecture:** Pure-math layers (`economy.ts`, `recipes.ts`, …) get TDD fixes — failing vitest first, minimal fix, suite green. Render/UI-layer fixes follow the codebase's established patterns (signature-gated rebuilds, `populatedKey()` guards, `destroy()` on detach). Each task below is a file-disjoint batch executed by one implementer subagent; batches are serialized onto master.

**Tech Stack:** Vite 5 + TypeScript strict + PixiJS 8 + vitest. `npx vitest run <file>` per-file; `npx tsc -b --noEmit` (or `npm run build`) for typecheck. NO `npm run dev`/service restarts needed.

**Conventions for every task:**
- TDD where the layer is pure: write the failing test, see it fail, fix, see it pass.
- After the batch: run every test file touching the modified sources + `npx tsc -b`.
- One commit per finding (or per tightly-coupled finding group), message style `fix(<area>): <what> (§<spec-ref> if applicable)`.
- Commit trailer: implementer identity (`Co-Authored-By: Kimi K2.6 <noreply@kimi.com>` for kimi; `Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>` for claude subagents). One primary-author trailer per commit.
- NEVER reduce existing test coverage; update tests only where the plan explicitly says the test pins wrong behavior.
- Do not touch findings marked REPORT-ONLY (Appendix A).

---

## Task 1: Persistence — perf-clock rebasing & import gaps (route: kimi)

**Files:** Modify `src/persistence.ts`; Test `src/persistence.test.ts`.

Findings:

- [ ] **1.1 (HIGH)** `persistence.ts:901-904` — satellites are copied through on load without `perfShift`, but `Satellite.movingTo.arrivalMs` is minted in the perf-clock domain (`orbital.ts:335,707`) and consumed by `tickSatMovement` (`orbital.ts:720`). A satellite saved mid-transit stays "in transit" ~the previous session's uptime after reload. Fix in the satellites map:
  ```ts
  satellites: snapshot.world.satellites.map((s) => ({
    ...s,
    buffer: (s.buffer ?? []).slice(-(s.bufferCap ?? SAT_BUFFER_CAP)),
    ...(s.movingTo ? { movingTo: { ...s.movingTo, arrivalMs: s.movingTo.arrivalMs + perfShift } } : {}),
  })),
  ```
  Test: serialize a world holding a satellite with `movingTo.arrivalMs = 5000`, deserialize with a nonzero perfShift (use the same shift-threading style as the existing drone/route perfShift tests), assert `arrivalMs` shifted. The existing round-trip test at `persistence.test.ts:1318-1343` uses perfShift 0 and cannot catch this.
- [ ] **1.2 (MED)** same lines — `buffer.slice(-SAT_BUFFER_CAP)` truncates to the global 100, but runtime cap is per-sat (`sat.bufferCap ?? SAT_BUFFER_CAP`, `orbital.ts:567-568`; skill multiplier ≥1 bakes caps >100 at launch, `orbital.ts:338`). Fix: slice to `-(s.bufferCap ?? SAT_BUFFER_CAP)` (included in 1.1's snippet). Also the defensive `(s.buffer ?? [])` so a buffer-less sat blob can't throw and nuke the whole save. Test: sat with `bufferCap: 150` and 150 buffer entries round-trips all 150.
- [ ] **1.3 (MED)** `persistence.ts:1125` — `isValidSaveSnapshot` pins `v['v'] !== SCHEMA_VERSION`, so Settings→Import rejects every older-but-migratable export, while the IDB load path migrates v7+. Fix: `!SUPPORTED_LOAD_VERSIONS.has(v['v'])`. Test: a minimal v-previous-version snapshot object passes `isValidSaveSnapshot`.
- [ ] **1.4 (LOW)** `persistence.ts:933` — `commPackets` copied without shifting perf-domain `generatedMs` (`orbital.ts:64`). No logic consumer today; shift it anyway for domain consistency: map packets to `{ ...p, generatedMs: p.generatedMs + perfShift }`. Cover inside 1.1's test.

Verification: `npx vitest run src/persistence.test.ts src/persistence-load.test.ts && npx tsc -b`.

## Task 2: Orbital — launch costs & per-frame hazard rolls (route: claude)

**Files:** Modify `src/orbital.ts`, possibly `src/main.ts` (threading dt); Test `src/orbital.test.ts`.

- [ ] **2.1 (HIGH)** `orbital.ts:279-307` — inventory deduction happens AFTER the launch success roll, so every failed launch is free. SPEC §14.7: "Fuel and the launch payload are lost." Move the deduction loop above the roll (still after target validation — invalid-target must NOT consume). The test at `orbital.test.ts:318-322` pins the wrong behavior ("Resources should NOT be consumed on failure") — rewrite it to assert consumption on failure, keep a test that invalid-target/validation failures do NOT consume.
- [ ] **2.2 (HIGH)** `orbital.ts:941-1007` — `tickScannerDiscovery` rolls `rng() < p` once per render frame (main.ts ticker), so discovery probability is frame-rate dependent and ~6%/sec at 60fps, gutting the §14.5 dwell ramp ("deep-orbit islands may take hours"). Convert to a per-time hazard: roll `pTick = 1 - (1 - p) ** (tickDeltaMs / SCANNER_REF_TICK_MS)` with the reference tick defined as 1000 ms so the existing per-tick constants keep a calibrated meaning (median time-to-discovery for a 1000 ms cadence stays what the current constants give at that cadence). Thread the real `tickDeltaMs` from the caller. Test: closed-form — assert `(1-p_frame(16.7ms))^60 ≈ (1-p_1s)` within 1e-9 (no RNG sampling).
- [ ] **2.3 (MED)** `orbital.ts:807-858` — `tickDebris` has the same per-frame roll bug (`debrisHitProbability` per call). Apply the same dt-scaled hazard with the same reference tick. Same closed-form test style.

Verification: `npx vitest run src/orbital.test.ts && npx tsc -b`.

## Task 3: Economy core integrator (route: claude — most capable; this is the riskiest batch)

**Files:** Modify `src/economy.ts`, `src/rate-history.ts`, `src/main.ts:1889` (one call site); Test `src/economy.test.ts` (+ a small rate-history block where natural).

- [ ] **3.1 (MED)** `economy.ts:844` + `:1814` — `layerConditionalBonuses(…, nowMs)` receives perf-domain time but `during-night` evaluates `realPhaseName` (wall/astronomical domain). The file already threads `solarClockMs = t + wallOffset` for exactly this. Fix: pass the wall-domain clock (`solarClockMs ?? t` in `computeRates`; per-segment `t + wallOffset` at the `:1814` advanceIsland layering — NOT end-of-advance `nowMs`). Also `main.ts:1889` HUD call leaves the param undefined — make the HUD's computeRates call supply the same wall-anchored value so displayed rates match applied rates. Test: a conditional `during-night` bonus evaluated inside `advanceIsland` over a range crossing a phase boundary applies the boundary-correct multiplier per segment (construct a synthetic graph node; the catalog's only conditional node is networked-based).
- [ ] **3.2 (LOW)** `economy.ts:1795-1809` — `aiCoreCrafted`/`ascendantCoreCrafted`/tutorial-gate flips fire on computed rates before `dtSec > 0` is known (a zero-length forced segment can grant T5/T6 access with zero production). Move the flips inside the `dtSec > 0` branch. Test: zero-length segment does not flip; positive segment does.
- [ ] **3.3 (LOW)** `economy.ts:445-457` — `cap()` early-returns 0 on `nominal === 0` BEFORE reading `state.starterInventoryGrace[r]`, making §12.4 grace dead at zero nominal cap (graced stock destroyed by the first applyRates clamp). Move the early return after the grace `Math.max`. Test: state with `storageCaps.x = 0`, `starterInventoryGrace.x = 50` → `cap()` returns 50 (and respects `ignoreGrace`).
- [ ] **3.4 (MED)** `economy.ts:1818-1826`/`:1918-1926` — battery boundary lacks a sub-unit epsilon: float residue in `batteryStoredWs` after a depletion-bounded segment can produce `nextBatteryMs == t` (zero-length segment), tripping the `segEndMs <= t` force-jump that skips ALL remaining integration. Mirror the inventory-side guard: treat `batteryStoredWs < 1` (1 Ws) as empty for boundary/cover decisions, and zero the residue when discharge leaves `< 1`. Test: stored = 1e-9 with a deficit — `advanceIsland` over a long window integrates the whole window (inventory moves), no freeze.
- [ ] **3.5 (MED)** `economy.ts:1258-1260` + `:1918-1926` — the battery deficit-cover and charge/discharge run unconditionally, but the in-file contract (lines ~1266-1268) says unified cable components balance at network level. Gate BOTH on `!(ctx?.cableComponent?.unified)`. Test: unified ctx + local deficit → `batteryStoredWs` unchanged across advance; non-unified keeps current behavior (existing battery tests stay green).
- [ ] **3.6 (MED)** `economy.ts:1465-1488` — `findNextCapEvent` skips events when `headroom < 1` / `current < 1` (anti-freeze workaround). Consequence (documented by the SKIPPED test `economy.test.ts:1865`): slow recipes overproduce/consume-from-nothing for a whole segment — up to hours offline. Fix: instead of `continue`, emit the event clamped to `Math.max(eventTimeMs, tMs + 1)` (1 ms minimum segment) so the boundary lands and `applyRates`' exact clamps zero the residue; the next `computeRates` then sees exact-cap/exact-0 and stalls the producer/consumer — no freeze. Re-enable the skipped test at `economy.test.ts:1865` (Cryogenic Compute Center 1/5400s) and make it pass. Run the FULL economy suite — highest-regression-risk change in the plan; if any existing anti-freeze regression test fails, stop and report rather than weaken it.
- [ ] **3.7 (MED)** `economy.ts:1100` vs `:1331` — pass-2 `tentSupply` is built from bare `baseRate`, but pass-4 applies `mf` (maintenance), `toxMul`, `buildingBonus` per building — flow-through consumers get inputs that were never produced (mf=0.5 producer grants 2× too much `inputAvail`). Fold the per-building factors computable in pass 1 (`mf × toxMul × buildingBonus`) into the tentative rate used for `tentSupply` and pass-2's `nominalRate`, leaving island-uniform factors (`accelMul`, `varianceFactor`) and the deliberate `pf` asymmetry post-applied as today. Test: producer at mf 0.5 + zero-stock consumer → consumption ≤ actual production over the segment.
- [ ] **3.8 (LOW)** `rate-history.ts:62-69` — `pruneRateBuffer` keeps an arbitrarily-old pre-gap sample after a hidden-tab gap, diluting displayed rates for up to a full window. Fix: when `buffer[0]` is older than `2 × RATE_WINDOW_MS` and ≥2 in-window samples remain, drop it. Test: hours-old head + fresh samples prunes the head; steady-state buffers keep the one-past-edge sample.

Verification: `npx vitest run src/economy.test.ts src/hud.test.ts && npx tsc -b`. Full `npm test` before finishing the task.

## Task 4: Heat & maintenance gating (route: claude; runs AFTER Task 3)

**Files:** Modify `src/heat.ts`, `src/maintenance.ts`, `src/economy.ts` (heat/maintenance call sites only); Test `src/heat.test.ts`, `src/maintenance.test.ts`, `src/economy.test.ts`.

- [ ] **4.1 (MED)** `heat.ts:136-263` + `economy.ts:1391-1412` — Coal Furnace serves heat regardless of coal stock; once coal hits 0 the burn clamps away and consumers keep full-rate production forever (free heat). SPEC §5.1: active iff "direct fuel-consumption stockpile if applicable". Fix: gate fuel-burning heat service on fuel availability — when the serving island's coal stock is 0 AND net coal flow can't cover the burn, mark the furnace's consumers heat-less (or throttle to the coverable fraction) the same way the existing throttle pass does; `findNextCapEvent` must emit a coal-depletion boundary while a furnace is burning (Task 3.6's fix makes sub-1 boundaries land). Test: island with 1 coal + furnace + heat consumer: consumer produces while coal lasts, stalls after, coal pinned at exactly 0, no production accrues afterward.
- [ ] **4.2 (LOW)** `heat.ts:207-212` vs `:253-259` — consumers flipped heat-less by the throttle pass (`next < MIN_HEAT_FACTOR`) still count in `coalConsumersByFurnace`, inflating the burn. SPEC §5.2 bills per consumer *served*. Decrement when the throttle pass flips a coal-assigned consumer to `hasHeat=false`. Test: furnace + 2 consumers, 1 throttle-stalled → burn = `coalPerCycle × 1 / COAL_CYCLE_SEC`.
- [ ] **4.3 (LOW)** `heat.ts:199-205` — coal-source pick is lowest-id; SPEC §5.2: lowest cost-per-cycle, id tie-break. Sort by `(heatSource.coalPerCycle, id)`. Keep `heat.test.ts:176-189` (equal costs) and add a differing-cost case.
- [ ] **4.4 (MED)** `maintenance.ts:253-271` + `economy.ts:1953` — `pickMostDegradedTarget` doesn't skip `disabled`/`invalid` buildings (a disabled degraded building soaks materials or blocks ALL auto-maintenance); `invalid` buildings also still accrue `operatingMs` (accrual loop filters under-construction/disabled/non-productive but not `invalid`). Fix both. Tests: disabled degraded building not targeted and doesn't block siblings; invalid building accrues no operatingMs.

Verification: `npx vitest run src/heat.test.ts src/maintenance.test.ts src/economy.test.ts && npx tsc -b`.

## Task 5: Recipe/building data tables (route: kimi)

**Files:** Modify `src/recipes.ts`, `src/building-defs.ts`, `src/recipe-graph.ts`, `src/recipe-density.ts`, `scripts/gen-cyclesec.ts`; Test `src/recipe-graph.test.ts`, `src/recipes.test.ts`, `src/building-defs.test.ts`, `src/balance.test.ts`.

- [ ] **5.1 (MED)** `recipe-graph.ts:44-49` — local `ownerOf` misses 65 recipe keys (all `skill_forge_*`, `sheet_mill`, `steel_mill_from_scrap`, `nodule_concentrator_re/_co`, `vent_mineral_refinery_exotic/_tritium`) → modal rows show tier 0, no gates, raw machine keys. Fix: delete `ownerOf`, use `buildingForRecipe` from `recipe-density.ts:117`. Test: every key of `RECIPES` resolves to a def in `BUILDING_DEFS` via the function `buildRecipeTableRows` uses.
- [ ] **5.2 (MED)** `recipe-graph.ts:85-88` — tier-gate label formula `(def.tier - 1) * 5 + 1` contradicts `tierForLevel` (skilltree.ts:298-304: thresholds 5/15/30/50, SPEC §9.2). Fix: `const lvl = [0, 1, 5, 15, 30][def.tier] ?? 1;` with a comment pointing at `tierForLevel`. Test: T2→"L≥5", T3→"L≥15", T4→"L≥30".
- [ ] **5.3 (HIGH)** `recipe-density.ts:158-160` + `scripts/gen-cyclesec.ts:31` — near-zero-mass outputs collapse derived cycleSec to ~1 s: `antimatter_refinery` cycleSec 1 (SPEC pins a **10-min cycle** → 600), `particle_accelerator` 1, `drilling_rig` 1.5 (file's own comments reference 800s). These top XP/sec by orders of magnitude. Fix: in `shouldDeriveCycleSec`, exclude recipes with `outputKg(recipe) < 0.01`; hand-pin `antimatter_refinery.cycleSec = 600` (spec), `drilling_rig.cycleSec = 800` (in-file comment), `particle_accelerator.cycleSec = 600` (match the antimatter-tier slow-cycle convention; comment the choice). Update the stale comments at the touched entries. Tests: assert the three pinned values; assert `shouldDeriveCycleSec` rejects sub-0.01kg outputs.
- [ ] **5.4 (MED)** `building-defs.ts:2576-2592` — `bulk_concrete_plant` is a dead def (no RECIPES entry, no archetype, consumes 2000 kW producing nothing; header says "100 concrete / 60 s"). Fix: add a RECIPES entry producing `concrete: 100` per 60 s with inputs scaled ×(100/output) from the existing base concrete recipe, plus the `BUILDING_ARCHETYPE` mapping mirroring the base concrete building's. Add the missing guard test: every `BUILDING_DEFS` entry in a recipe-bearing category has a RECIPES entry (allowlist legitimately recipe-less categories; if OTHER dead defs surface, report them, don't fix here).
- [ ] **5.5 (MED)** `recipes.ts` XP_WEIGHT vs SPEC §9.1/§6: `helium_3` 100→**30** (§6.4 T3), `carbon_fiber` 100→**30** (§6.4 T3), `oxygen` 3→**30**, `argon` 3→**30** (§6.4 T3), `battery` 30→**10** (§6.3 T2), `pcb` 10→**30** (§6.4 T3), `solder` 10→**3** (§6.2 T1). Update tests pinning old weights (`recipes.test.ts`, `trade.test.ts` — §9.8 pricing derives from weights).
- [ ] **5.6 (LOW)** `recipes.ts:765-777` — `chromium_ingot`/`nickel_ingot`/`tungsten_ingot` weight 30 → **10** (ingot one rung below its alloy, matching iron/manganese/zinc chains).
- [ ] **5.7 (LOW)** stale load-bearing comments contradicting values (one `docs(recipes)` commit): `nuclear_reactor` "600s" vs 200; `cell_press` "40s" vs 537495.7; `heavy_logger` "3×" vs 4.0×; `deep_mine` "3×" vs 1.5×; `*_sat_assembly` "10-min" vs ~110 days; ocean extractors "60-120s" vs 4.1 — fix each comment to describe the actual value (do NOT change values beyond 5.3's three pins).

Verification: `npx vitest run src/recipe-graph.test.ts src/recipes.test.ts src/building-defs.test.ts src/balance.test.ts src/mass-balance.test.ts src/trade.test.ts src/recipe-density.test.ts src/cyclesec.test.ts src/cyclesec-guards.test.ts && npx tsc -b`.

## Task 6: Drones — offline windows, corridors, destruction (route: claude)

**Files:** Modify `src/drones.ts`, `src/main.ts` (load-time catch-up), `src/drones-ui-helpers.ts`, `src/drones-ui.ts` (efficiency threading); Test `src/drones.test.ts`, `src/drones-ui-helpers.test.ts`.

- [ ] **6.1 (HIGH)** `drones.ts:611` + main.ts ticker init — first frame after a reload clamps `segStartMs` to load-time `performance.now()`, so the corridor flown while the tab was closed is never scanned (a flight that completed offline reveals NOTHING). SPEC §15.5/§11.6. Fix: persistence already rebases `launchTime` etc. by perfShift; capture the rebased save-time tick (check what `deserializeWorld` exposes; if nothing, derive from the islands' rebased `lastTick` min) and run ONE catch-up `tickDrones(world, now, rebasedSaveTimeMs)` after load, before the ticker starts. Test (pure layer): drone with full flight inside [1000, 5000]; `tickDrones(world, 6000, 900)` reveals the corridor; plus a clamp test for in-session pauses.
- [ ] **6.2 (MED)** `drones.ts:616-631` — T5 path-drawn corridor sampling only inserts the apex; waypoints inside the segment are skipped, so coarse ticks scan straight chords. Fix: for `d.waypoints.length >= 2`, insert every waypoint whose cumulative-distance time falls strictly inside `(segStartMs, segEndMs)` (outbound and reverse legs), like `rasterizeWaypointPathForWeather` (drones.ts:264-307) already does for weather. Test: L-shaped 3-waypoint path, one tick spanning the flight → revealed cells match the polyline, not the chord.
- [ ] **6.3 (MED)** `drones.ts:697-754` — destruction is rolled at return time, but live scanning runs all flight, so a drone fated to die at outbound cell 3 still reveals its whole route. SPEC §11.7: fate decided at launch. Fix: roll destruction at dispatch, store the destruction `entryMs` on the drone (optional field — confirm persistence round-trips it via plain spread; missing field → current return-time fallback), clamp `segEndMs` to it in the scan loop, keep return-time bookkeeping driven by the stored fate. Test: drone fated to die outbound reveals only cells up to the death cell.
- [ ] **6.4 (LOW)** `drones.ts:673` — ordinary island discovery runs over `expandedCorridor` (rare-bias ring); SPEC limits the bias to rare/unique islands. Pass `corridor` at line 673 (line 668 keeps `expandedCorridor`). Test: non-rare island only inside the ring NOT discovered; rare one is.
- [ ] **6.5 (LOW)** `drones.ts:253` — Probability Engine bias counts under-construction/invalid buildings. Filter with `isOperationalBuilding` (pattern: antenna.ts:52). Test: under-construction engine grants no bias.
- [ ] **6.6 (LOW)** `drones-ui-helpers.ts:40,50` — T5 range gate and fuel charge use raw `DRONE_T5_EFFICIENCY`, ignoring the skill multiplier the engine applies (`drones.ts:396`). Fix: both helpers accept `efficiencyMul = 1`; `drones-ui.ts` passes `effectiveSkillMultipliers(origin).droneFuelEfficiency`. Test: helpers scale with the param.

Verification: `npx vitest run src/drones.test.ts src/drones-ui-helpers.test.ts && npx tsc -b`.

## Task 7: Routes & route rendering (route: claude)

**Files:** Modify `src/routes.ts`, `src/route-cargo.ts`, `src/routes-renderer.ts`; Test `src/routes.test.ts`, `src/route-cargo.test.ts`, `src/routes-renderer.test.ts`.

- [ ] **7.1 (MED)** `routes-renderer.ts:216` — `.rotate(angle).translate(-offsetPx, 0)` appends translation AFTER rotation: UV phase advance is `offset·cos(angle)`, so vertical routes never animate (verified against Pixi 8 `generateTextureFillMatrix`/`buildUvs`). Fix: `.set(1,0,0,1,0,0).translate(-offsetPx, 0).rotate(angle)`. Test: matrix `tx/ty` for angle 90° equals the rotated translation (pure matrix assertion).
- [ ] **7.2 (MED)** `route-cargo.ts:76` — waterfall mode clamps desired to `e.sourceAvail`, defeating the §15.4 capacity-proportional Phase-2 split that `routes.ts:679-684` deliberately preserves (mixed contention skews shares). Fix: report the unclamped capacity share as `desired` for Phase-2 scaling; keep the clamp for intra-route spill math. Test: source avail 5, waterfall routes capacity 1/s and 10/s → split ≈0.45/4.55, not 0.83/4.17.
- [ ] **7.3 (LOW)** `routes.ts:819` — instant-transit deposit omits the `{ ignoreGrace: true }` that `deliverArrivals` (line 589) and `destinationHeadroom` (line 467) pass per §12.4. Add it. Test: instant delivery cannot fill into grace headroom.
- [ ] **7.4 (LOW)** `routes-renderer.ts:85` — per-route cacheKey omits endpoint positions; geometry goes stale if an island centre moves. Add endpoint world coords to the key. Test: changing an endpoint busts the cache entry.

Verification: `npx vitest run src/routes.test.ts src/route-cargo.test.ts src/routes-renderer.test.ts && npx tsc -b`.

## Task 8: Weather clock domain & field coherence (route: claude — cross-cutting)

**Files:** Modify `src/main.ts`, `src/routes.ts`, `src/drones.ts`, `src/settlement.ts`, `src/weather.ts` (call-path threading only), `src/hover-tooltip.ts`, `src/weather-overlay.ts`; Test `src/weather.test.ts`, `src/routes.test.ts`.

- [ ] **8.1 (MED-HIGH)** Every production `weather()` sample runs on `performance.now()`-domain timestamps, so the weather timeline restarts at t≈0 every page load — sessions replay the same initial dwells; weather never drifts offline (SPEC §15.1/§2.6 violated). main.ts:1762-1767 documents and fixes this exact pathology for day-night via `wallOffset`. Fix: export one helper (e.g. `weatherClockMs(perfTs, wallOffset)`) and sample weather at `perfTs + wallOffset` at every call site (overlay refresh, hover tooltip, route dispatch capacity, route arrival losses with stored `dispatchTime`, vehicle/drone destruction with stored `entryMs`/`launchTime`). `weather()` itself stays pure. Comment the accepted semantics: perf-shifted in-flight timestamps map to post-gap wall times on reload. Tests: existing weather tests stay green; test the helper + one call-path consistency assertion.
- [ ] **8.2 (MED)** Weather field incoherence: `deliverArrivals` samples with CO₂ but no biome; `routeCapacityMultiplierForWeather` and `rollVehicleDestruction`/drone rolls sample with neither — same cell, same instant can be "storm" for losses but "clear" for capacity/destruction; §7.3 CO₂ amplification never affects vehicle fates. Fix: thread `biomeForCell(...)` + `sumIslandCo2(world)` into all these paths (signature additions with defaults fine). Test: high CO₂ forcing storm → capacity multiplier and destruction roll both see storm.

Verification: `npx vitest run src/weather.test.ts src/routes.test.ts src/drones.test.ts src/settlement.test.ts && npx tsc -b`. Full `npm test` before finishing (cross-cutting batch).

## Task 9: Skill tree — gating, crystals, refunds (route: claude — most capable)

**Files:** Modify `src/skilltree.ts`, `src/skilltree-graphview.ts`, `src/skilltree-crystals.ts`, `src/tier-reset.ts`; Test `src/skilltree.test.ts`, `src/skilltree-crystals.test.ts`, `src/tier-reset.test.ts`, `src/skilltree-catalog.test.ts`.

- [ ] **9.1 (HIGH)** `skilltree.ts:1401-1404` — `costToUnlock` paths through `mode: 'and'` keystone-prereq edges, so keystones unlock through any ONE prereq; `buyKeystone`/`canBuyKeystone` are dead code. SPEC §9.3 requires the AND gate. Fix: skip `e.mode === 'and'` edges in pathing adjacency; route graphview keystone purchases through `canBuyKeystone`/`buyKeystone`; `nodePurchaseStatus` reports unmet-prereq keystones as locked. Tests: 1-of-2 prereqs → NOT purchasable, not auto-owned as path intermediate; both prereqs → purchasable.
- [ ] **9.2 (HIGH)** `skilltree.ts:298-360` — `depthTierEligible` caps at `tierForLevel` max 5, so all 21 depth≥8 nodes (incl. 6 keystones) are permanently locked; SPEC §13.2 unlocks T6 via Ascendant Core + Spaceport flags. Fix: thread the T6 predicate (the flags the callers already hold near `aiCoreCrafted`/`ascendantCoreCrafted`) into the gate. Test: level-50 + t6 flags → depth-8 purchasable; without flags → tier-locked.
- [ ] **9.3 (HIGH)** `skilltree.ts:718-736` — `bindCrystal` never owns the synthetic socket node, so every crystal mini-tree node is `unreachable` — the feature is dead after binding. Fix: `bindCrystal` adds the socket node id to `unlockedNodes` (cost 0); unbind removes it. Test: bind → core purchasable; unbind → socket ownership gone.
- [ ] **9.4 (HIGH)** `skilltree.ts:681-708` + `skilltree-crystals.ts:61` — purchase charges Σ edge costs but unbind refunds Σ node costs + Σ edge costs → infinite SP mint (bind/buy-all/unbind nets +7 SP on the T1 mining crystal). Fix: refund exactly what was charged (Σ owned-edge costs; core's socket edge is 0). Fix `computeMiniTreeRefund` to match. Test: bind → buy all → unbind is SP-neutral.
- [ ] **9.5 (MED)** `tier-reset.ts:139-143` — refund sums node costs; purchases charge edge costs (bridges hand-priced ≠ node cost; crystal mini-tree nodes not in catalog → refund 0). SPEC §9.7 "All spent skill points refunded". Fix: refund Σ costs of traversed `unlockedEdges` + root-node purchase costs + crystal charges (reuse 9.4's accounting). Test: chain + bridge + crystal purchase sequence → post-reset SP exactly equals pre-purchase SP.
- [ ] **9.6 (MED)** `skilltree.ts:1602-1611` vs `skilltree-graphview.ts:50-58` — two branch-spend formulas (edge-cost sum vs node-cost sum; root purchases invisible to the engine's). Fix: engine `spentInBranch` counts root purchases (no edge → include node cost), export it, graphview consumes it. Test: root-only ownership activates a bridge at threshold == root cost in BOTH engine pathing and the exported fn.
- [ ] **9.7 (MED)** `skilltree.ts:529-536` — anchor fallback picks alphabetically (`inputEff`), anchoring three depth-3 notables (`smelting.notable.refractoryLining`, `chemistry.notable.greenChemistry`, `electronics.notable.satBandwidth`) to depth-6 chain tails (T5-locked); dependent keystones inherit the lock. Fix: anchor to the chain node with `depth === targetDepth - 1` (or nearest below) across the sub-path's chains. Test: each notable purchasable at T3 (level 15) with its chain owned.
- [ ] **9.8 (MED)** `skilltree.ts:233,244-246` — `patronage` under `ocean`; SPEC §9.3 puts Patronage under Logistics (4 sub-paths per branch). Fix: move it in `SUBPATH_BRANCH`/`BRANCH_SUBPATHS`; re-key affected `BRIDGE_CATALOG` thresholds. Full skilltree suite; update only counts deriving from the wrong assignment.
- [ ] **9.9 (LOW)** `skilltree-crystals.ts:80-84` etc. — descriptions contain literal `Math.round(...)` (missing `${}`) in 19 of 20 factories. Fix all. Test: no catalog description contains "Math.round".
- [ ] **9.10 (LOW)** `skilltree.ts:1072,1077,1090,1094,1288-1301` — aura amplification skipped for additive accumulations (`parallelBuildBonus`, `queueCapAdd`, `tradeReachAdd`, `tradeSpreadShiftAdd`, `launchSuccessBonus`); `launch.notable.padRedundancy`'s aura does nothing. SPEC §9.3 has no additive carve-out. Fix: `+= node.magnitude * amp`; thread `computeAuraAmplifiers` through `launchSuccessBonus`. Test: padRedundancy aura amplifies an adjacent launch-success node.
- [ ] **9.11 (LOW)** `skilltree.ts:718-736` — `bindCrystal` doesn't validate `eligibleSubPaths` (only the UI picker filters). Add the engine-level check (throw on mismatch). Test: mining crystal → smelting socket throws.

Verification: `npx vitest run src/skilltree.test.ts src/skilltree-crystals.test.ts src/tier-reset.test.ts src/skilltree-catalog.test.ts src/skilltree-budget.test.ts src/skilltree-integration.test.ts src/skilltree-graph.test.ts && npx tsc -b`.

## Task 10: Sim misc — toxicity, daynight, construction, format (route: kimi)

**Files:** Modify `src/reactor-toxicity.ts`, `src/daynight-tint.ts`, `src/construction.ts`, `src/placement.ts` (initial-duration storage only), `src/format.ts`; Test matching `.test.ts` files.

- [ ] **10.1 (MED)** `reactor-toxicity.ts:76-83` — `break` after the first triggered hour makes outcomes depend on catch-up granularity (one 24h advance ≤1 event; 24×1h advances can trigger several), violating the module's own granularity-invariance contract. Fix: remove the `break`; the just-written `toxicityExpiryMs` gates subsequent hours inside the same loop. Test: 24h in one call vs 24 hourly calls, seed triggering ≥2 events → identical outcomes.
- [ ] **10.2 (LOW)** `daynight-tint.ts:97-101` — at the phase-0.0 boundary, pre-boundary p≈0.9966-1.0 gives pp≈1.0+ (never negative), the wrap doesn't fire, and the night→dawn crossfade pops to full dawn early. Fix: `pp = ((p - start) % 1 + 1) % 1`. Test: `tAt(0.9966) ≈ 0` for the 0.0 boundary (mirror the 0.75-boundary test).
- [ ] **10.3 (LOW)** `construction.ts:54-58` + `placement.ts:658` — progress divides remaining by the UN-multiplied base, so Robotics `constructionTimeMul = 2` shows 50% instantly. Fix: store `constructionTotalMs` on the building at placement (optional field; plain spread persists it; absent → fall back to `upgradeConstructionMs(def, 0)`); `constructionProgress` divides by it. Test: mul-2 placement starts at 0 progress; old-shape building still computes.
- [ ] **10.4 (LOW)** `format.ts:12` — `fmtPower(0.9996)` renders "1000 W". Fix: round first, then pick the unit. Test: `fmtPower(0.9996) === '1 kW'`; `fmtPower(0.999) === '999 W'`.

Verification: `npx vitest run src/reactor-toxicity.test.ts src/daynight-tint.test.ts src/construction.test.ts src/format.test.ts src/placement.test.ts && npx tsc -b`.

## Task 11: Island merge integrity (route: claude)

**Files:** Modify `src/island-merge.ts`; Test `src/island-merge.test.ts`.

- [ ] **11.1 (HIGH)** `island-merge.ts:151-157` — absorbed buildings keep ids minted from island-LOCAL coords (`placed-${x},${y}`) → duplicate ids after merge; id-keyed consumers demolish/upgrade the wrong building, `clusterBonusMuls` overwrites, `ignoreBuildingId` exempts both. Fix: re-mint copied ids from shifted coords (`placed-${b.x + offsetX},${b.y + offsetY}`); guard residual collisions by suffixing the absorbed island id. Re-point anything referencing the old id (grep consumers: routes' building refs, drone trails). Test: merge two islands each holding `placed-0,0` → unique ids; routes referencing the absorbed building still resolve.
- [ ] **11.2 (MED)** same loop — ocean platforms keep `anchorIslandId` of the absorbed island (deleted in step 8) → `oceanPlatformPausedReason` 'anchor-depopulated' forever. Fix: rewrite to `absorber.id` when it equals `absorbed.id`. Test: post-merge buoy not paused.
- [ ] **11.3 (MED)** `performMerge` drops absorbed `tileOverrides` (paid conversions revert; extractors invalidated). Fix: copy entries under shifted keys, absorber wins collisions. Test: absorbed override survives at shifted coords; `terrainAt` honours it.
- [ ] **11.4 (MED)** `island-merge.ts:162-170` — inventory transfer clamps to PRE-merge caps; absorbed storage buildings' capacity never credited. Fix: credit each operational absorbed storage building via `creditStorageCaps` (placement.ts:424) after the building copy, THEN transfer inventory. Test: absorbing an island with an operational crate raises the cap; transfer respects the raised cap.

Verification: `npx vitest run src/island-merge.test.ts && npx tsc -b`.

## Task 12: World/placement geometry (route: claude)

**Files:** Modify `src/placement.ts`, `src/world.ts`, `src/world-gen.ts`, `src/ocean-gen.ts`, `src/land-reclamation.ts`; Test matching `.test.ts` files.

- [ ] **12.1 (HIGH)** `placement.ts:277-280` — `validatePlacement` tests only the PRIMARY ellipse, so all merged-island (extra-ellipse) land is `out-of-bounds` forever (relocation too). SPEC §4.3: "inside one of the island's constituent ellipses". Fix: `islandInscribedAny(spec, t.x, t.y)` (exported from island.ts; biomes/terrain-modifier already use it). Test: building placeable on absorbed-constituent land post-merge.
- [ ] **12.2 (HIGH)** `world.ts:566` — `makeHomeIslandSpec` builds a bespoke `terrainAt` ignoring `tileOverrides`; terrain-modifier shots on home are no-ops until reload (persistence rehydrates via `attachTerrainAt`). Fix: route home through `attachTerrainAt`. Test: `applyTileOverride(home, 0, 0, 'ore')` → `home.terrainAt(0,0) === 'ore'` on a fresh world.
- [ ] **12.3 (MED)** `placement.ts:1218-1289` — `validateOceanPlacement` never checks existing ocean buildings; two platforms from different anchors can share a cell (the docblock's joint-prevention claim is false). Fix: walk populated islands' `oceanPlacement` buildings, reject cell-AABB intersection (new reason `'ocean-overlap'`; wire a label if reasons are user-visible). Test: second platform on the same cell from a different anchor rejected.
- [ ] **12.4 (MED)** `world.ts:859` vs `:882-900` — `generateOceanTerrain` runs only at boot; `ensureCellGenerated` mints islands with NO shallows ring or vents (ocean-gen's own R=2 contract violated; lazily-found volcanic islands can never host vent harvesters). Fix: extend `ensureCellGenerated` to run the per-island ocean passes for newly minted specs (per-island vent sub-stream `${seed}_ocean_vent_${id}` keeps determinism). Test: lazily generated island has its shallows ring; volcanic vents identical boot vs lazy for the same seed.
- [ ] **12.5 (LOW)** `world-gen.ts:229-245` — `overlapsAny` measures only primary ellipses; new procedural islands can overlap merged islands' absorbed constituents. Fix: iterate `islandConstituents(o)` (world.ts:188). Test: candidate overlapping only an extra-ellipse is rejected.
- [ ] **12.6 (LOW)** `land-reclamation.ts:59-74` — expansion cost = delta of PRIMARY-only `inscribedTileCount`; tiles already inside an extra ellipse are charged as new. Fix: delta over the union (count tiles where `islandInscribedAny` flips false→true). Test: expansion overlapping an absorbed ellipse charges only genuinely-new tiles.

Verification: `npx vitest run src/placement.test.ts src/world.test.ts src/world-gen.test.ts src/ocean-gen.test.ts src/land-reclamation.test.ts src/island.test.ts && npx tsc -b`.

## Task 13: UI input & window manager (route: claude)

**Files:** Modify `src/window-manager.ts`, `src/main.ts`, `src/graph-ui.ts`, `src/inspector-ui.ts` (rename input only); Test `src/window-manager.test.ts`, `src/input.test.ts`.

- [ ] **13.1 (HIGH)** `window-manager.ts:245,431-448` — any pointerdown inside a panel mints a degenerate `{0,0,0,0}` layout via `bringToFront`→`updatePanelLayout`; the resize handler clamps ALL panels to it → clicked panels collapse to 1×1 on window resize and the value persists. Fix BOTH: `bringToFront` only patches zRank when a layout entry exists; resize handler skips non-free-floating panels. Test: pointerdown on zone-anchored panel + resize → keeps zone layout; dragged panel still clamps.
- [ ] **13.2 (MED)** `main.ts:528-535` — keyup dropped while a text input is focused → held pan key released inside an input pans forever. Fix: run `releaseHandlers` unconditionally on keyup (releases are side-effect-free); keep the keydown gate. Test: press-pan, focus input, release → held flag cleared.
- [ ] **13.3 (MED)** `graph-ui.ts:310` — header × sets `panelHandle.setVisible(false)` but leaves module `visible=true` → next toggle is a no-op (Y twice to reopen). Fix: × goes through the same hide path. Test: close via × then `toggle()` returns true.
- [ ] **13.4 (LOW)** `inspector-ui.ts:440-444` — rename input Escape doesn't `stopPropagation`; main.ts's window handler also fires `dismiss-modal` and closes the inspector. Fix: `e.stopPropagation()` (pattern: inventory-ui.ts:497-499).
- [ ] **13.5 (LOW)** `main.ts:889-893` vs `input.ts:99,126` — action strip advertises `kbd: 'N'` for Drones; N is `toggle-skill-graph`, Drones is J. Fix strip to `'J'`; test asserts the strip kbd matches the registry binding (look it up, don't hardcode).

Verification: `npx vitest run src/window-manager.test.ts src/input.test.ts && npx tsc -b`.

## Task 14: UI per-frame rebuild / dead-click cluster (route: kimi)

**Files:** Modify `src/orbital-ui.ts`, `src/build-queue-ui.ts`, `src/hud.ts`, `src/inspector-ui.ts` (biome row only), `src/settlement-ui.ts`, `src/drones-ui.ts`, `src/weather-overlay.ts`; Test `src/hud.test.ts` + tsc/build for DOM-only modules.

Root pattern (documented in-repo at `routes-ui.ts:590-594` and `:991-995`): rebuilding DOM children every frame destroys click targets between mousedown and mouseup, and closes open native `<select>` popups. Fix pattern also in-repo: content-signature gating (`populatedKey()` in routes-ui, `padSig` in drones-ui).

- [ ] **14.1 (HIGH)** `orbital-ui.ts:616-617,536-555` — `bodyEl.replaceChildren()` every frame while visible; Upgrade/Arm-Launch buttons dead. Fix: content-signature gate the render.
- [ ] **14.2 (HIGH)** `build-queue-ui.ts:172-173` — body rebuilt every frame; ✕ cancel buttons dead. Fix: row cache keyed by building id; update progress in place.
- [ ] **14.3 (MED)** `hud.ts:526,557-561,769-772` — HUD body rebuilt every frame containing TIER-RESET and Inventory buttons. Fix: persistent buttons or signature-gated rebuild; values keep updating per frame.
- [ ] **14.4 (MED)** `inspector-ui.ts:1690-1732` — Universe-Editor biome buttons rebuilt every frame. Fix: build once; per-frame only update disabled/active styling.
- [ ] **14.5 (MED)** `settlement-ui.ts:825-829` — `rebuildSelectors()` every frame kills open dropdowns. Fix: `populatedKey()`-style signature guard (include island names so renames repaint).
- [ ] **14.6 (MED)** `drones-ui.ts:357-362` — '5-path' `<option>` only created when `canUsePathMode()` true AT MOUNT; refresh only toggles `disabled` on existing options → unlocking mid-session never surfaces T5 path mode. Fix: always append the option at mount; existing disabled toggle governs availability.
- [ ] **14.7 (LOW)** Pixi churn without `destroy()`: `drones-ui.ts:875-885`, `settlement-ui.ts:696-702`, `weather-overlay.ts:132` (correct pattern at building-alerts-overlay.ts:69-75). Fix: `for (const c of layer.removeChildren()) c.destroy({ children: true });`.
- [ ] **14.8 (LOW)** `drones-ui.ts:894-897` — ledger denominator hardcoded `/ 1`; busy model is per-pad. Fix: denominator = operational pad count on the active island.

Verification: `npx vitest run src/hud.test.ts src/inventory-ui.test.ts && npx tsc -b && npm run build`.

## Task 15: UI misc — inspector rates ctx, labels, ids (route: claude)

**Files:** Modify `src/inspector-ui.ts`, `src/placement-ui.ts`, `src/hud.ts` (option names), `src/main.ts` (ctx threading + id-scoped consumers); Test `src/hud.test.ts`, `src/placement-ui.test.ts`.

- [ ] **15.1 (MED)** `inspector-ui.ts:1315,1451` — inspector calls `computeRates(state, { terrainAt }, undefined, Date.now())`, dropping `modifierMul`/`ncBuff`/`activeBonusMul`/`cableComponent`/`solarBoost`/`geothermalActive` that main.ts threads — displayed rates contradict the BONUSES row and the HUD. Fix: add a `getRatesContext(islandId)` dep to InspectorDeps supplied by main.ts (extract main.ts's ctx assembly into a shared helper; don't duplicate), use it in both computeRates calls.
- [ ] **15.2 (LOW)** `placement-ui.ts:353-357` vs `:397-398` — "→ TARGET" terrain annotation appended to the previous frame's label then overwritten by the recompute — never visible. Fix: fold the target into the recomputed label. Test: with `activeTerrainTarget` set, painted label contains the target string.
- [ ] **15.3 (LOW)** `hud.ts:347-357,386` — island dropdown option names never repaint after rename (sig is id-only). Fix: update `name.textContent` in the per-frame option loop.
- [ ] **15.4 (MED)** `placement-ui.ts:219-221` + global consumers — `placed-${x},${y}` ids collide ACROSS islands; main.ts toggle-building-disable walks all islands and hits the first match; hover-suppression and drone-trail maps are id-keyed. Constraint: do NOT rewrite ids in existing saves. Fix consumers to scope lookups to the owning island (active island for toggle-disable; `(islandId, buildingId)` pairs where global maps exist — investigate each consumer: `drainRoutesForBuilding`, hover suppression main.ts:1216, drone trails). Mint NEW ids island-qualified (`placed-${spec.id}-${x},${y}`) so future placements are globally unique while old ids keep working (consumers treat ids as opaque). Test: two islands with same-coord buildings — toggling disable on island B's does not touch island A's.

Verification: `npx vitest run src/hud.test.ts src/placement-ui.test.ts && npx tsc -b`. Full `npm test` after this final batch.

---

## Appendix A — REPORT-ONLY (do not implement; goes in the final HTML report)

| # | Finding | Why deferred |
|---|---|---|
| A1 | `steel_mill_from_scrap` yield parity (§6.7) | Tracked Phase 10 recalibration (TODO.md:161, skip at recipes.test.ts:329) |
| A2 | rev-16 §12.9.5 reachability (battery_bank ≤45 min) violated | Tracked Phase 7 tutorial restructure (reachability.test.ts todo) |
| A3 | Phase-10 stale test fixtures (8 skips: blast furnace/smelter/steel mill cohort) | Same Phase 10 batch |
| A4 | "deferred-rebalance §06" skips (battery-ladder/sonar-buoy/economy ×3) cite a non-existent spec doc; fixtures assume pre-rebalance coal_gen | Needs a fixture-recalibration decision, not a source fix |
| A5 | Skilltree filler cost curve ≠ SPEC §9.3 `round(1.5^(depth-1))` ramp | Balance decision — archetype curves look deliberate |
| A6 | Catalog totals 363/86/31 vs SPEC's "357 (246+81+30)" | SPEC text update; spec is locked — needs user sign-off |
| A7 | Ocean extractor pacing (4.1 s cycles, ~490× T1 Mine XP/s) | Conforms to the density-table spec doc; balance review |
| A8 | Weather visibility in tiles vs SPEC §2.6 cells | Docstring + tests pin the tile choice as deliberate retune |
| A9 | `rerollModifiers` bakes `Date.now()` into seed (non-reproducible; no island id) | Possibly intended entropy; determinism policy decision |
| A10 | Lattice unified pool: consumption never drains partner islands (matter-from-nothing at §13.3) | Feature marked "P" (partial); needs a designed drain/reconciliation pass, not a patch — flag prominently in the report |
| A11 | mass-balance backlog (202 deliberately-scoped skips) | Acknowledged data-quality debt outside the binding gate |
| A12 | routes-renderer perf test skip (2.48 ms vs 0.76 ms baseline) | Needs a profiled re-measure; container variance suspected |
| A13 | Self-route XP loop guard (routes engine accepts from===to) | UI guards exist; engine guard is defense-in-depth — low value |
| A14 | Input-contract Escape/Enter cleanups (map-picker, pickers, hud) | Contract hygiene, no user-visible defect; several have documented exceptions |
| A15 | helicopter T1 vs SPEC §12.6; tutorial `invSeen` semantics; CO₂ retroactive dwell rewrite; ocean-gen enclosing-circle seam; 5-point cell-sampling graze | Speculative/design-intent items from the audits |

## Appendix B — execution notes for the orchestrator

- Task order: 3 before 4 (heat depletion boundary depends on 3.6). Tasks 1/5/10/14 are kimi-routable; the rest claude. Serialize ALL implementers (main.ts is shared by 6/8/13/15 — strictly one batch at a time).
- Reviewers: ALWAYS Opus via Agent tool (subagent-driven-development-patch). Reviewer briefs: launch `npm test` in background FIRST, then read the diff.
- Implementer commits land directly on master (user CLAUDE.md: subagent-driven development on master, no worktrees).
- After Task 15: full `npm test` + `npm run build`, re-check `git log --oneline` linearity, then the HTML report (writing-documentation skill → docs-hub publish).
